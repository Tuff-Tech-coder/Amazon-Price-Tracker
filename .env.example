# Copy to .env and fill in. .env is gitignored and must never be committed.
# Only needed for email alerts -- the tracker runs and logs prices without it.

# SMTP credentials for alert delivery.
# Gmail: use an App Password, not your account password.
SMTP_PASSWORD=

# Sender and recipient are set in config.json; only the password lives here.
